<%- include('../../partials/header.ejs') %>
<%- include('../../partials/navbar.ejs') %>
    <body>
        <div class="container">
            <hr>
            <div class="card mt-4">
                <div class="card-header">
                    <h2>Adicionar jogo novo</h2>
                </div>
                <div class="card-body">
                    <form action="/barcode/create" method="POST">
                        <input type="text" name="barcode" placeholder="Código de barras" class="form-control"><br>
                        <label>Nome do jogo:</label>
                        <select name="gameId" class="form-control">
                            <% games.forEach(game => { %>
                                <option value="<%= game.id %>"><%= game.title %></option>
                            <% }) %>
                        </select><br>
                        <button type="submit" class="btn btn-success btn-block">Criar</button>
                    </form>
                </div>
            </div>
        </div>
    </body>
<%- include('../../partials/footer.ejs') %>